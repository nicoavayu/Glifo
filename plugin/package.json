{
  "name": "plugin-uxp-premiere-mvp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "bundle": "esbuild src/panel/main.ts --bundle --format=iife --platform=browser --outfile=dist/panel/main.js",
    "build": "npm run typecheck && npm run bundle",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
